<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.freedotech.app.ztly.dao.DungouDao">
	 <!-- 查询一条历史数据 -->
    <select id="getOneHData" resultType="hashmap">
		select * from tunneling_runtime_1 where tunneling_id = 1
    </select>
     <!-- 查询一些历史数据 -->
    <select id="getSomeHData" resultType="hashmap">
		select * from tunneling_history_1 limit 0,30
    </select>
</mapper>